'''
Array multiplication follows matrices multipliucatin rule

Matrices multiplication rule,
matrice1    :   n X m   | (rows=n, columns=m)
matrice2    :   m X p   | (rows=m, columns=p)

matrice_product = matrice1+matrice2

matrice_product : n X p | (rows=n, columns=p)

...

# number of rows of one array must be equal to number of columns

array(rows,columns)
array_product = array1(a,b) * array2(b,c)
array_product(a,c)
'''

'''
// The matrix product can be performed:
    using the @ operator 
    or the dot function:
