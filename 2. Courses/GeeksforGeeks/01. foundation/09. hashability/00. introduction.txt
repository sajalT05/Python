// hashability: 
    1. feature of python, tells an object hs hash value or not.
    2. if hashable, can be used as a key in dictionary or element in set.

// object is hashable if it's hash value doens't change in the lifetime.

// built in hash methods:
__hash__()
hash()

// hash value depends upon value stored.

// inbuilt functions are not hashable.

// user defined functions are hashable.