// authentication is inbuilt in django

// handle authentication and authorization
    a. authentication: verifies a user who they claim to be.
    b. authorization: determines what an authenticated user is allowed to do.


// authorization
    a. users
    b. permissions
    c. groups
        i. labels and permissions to more than one users.
    d. configurable password hashing system.
    e. forms and view tools for loggin in users, or restricting content.
    g. pluggable backend system.